# Pipeline Financeiro — Python dependencies (human-edited source · ADR-254)
# NÃO instalar diretamente em prod/CI: use requirements.lock (gerado por
# `pip-compile --generate-hashes` em container linux/amd64).
# Regenerar: ver docs/reference/runbooks/python_dependencies.md

# PDF extraction
pdfplumber>=0.10
pikepdf>=8.0

# Spreadsheet parsing
xlrd>=2.0
openpyxl>=3.1

# HTML parsing (Itaú CDB HTML-as-XLS)
beautifulsoup4>=4.12

# YAML (E6 render, config)
PyYAML>=6.0

# Timezone handling
pytz>=2024.1

# LLM classification fallback (content-first classifier, D1=B in ADR)
# Required in backend: when content-regex confidence < 0.8, classify_document
# escalates to Claude via this SDK. Needs ANTHROPIC_API_KEY at runtime.
anthropic>=0.30

# Schema validation
jsonschema>=4.20

# Monte Carlo simulations (IF projector — N3)
# Teto de major por ADR-360: a política do numpy garante o bit stream do
# ``Generator`` só no mesmo build/ambiente e permite quebrá-lo em release X.Y —
# bump é evento de rebaseline de snapshot, não upgrade silencioso do Dependabot.
numpy>=1.26,<3
