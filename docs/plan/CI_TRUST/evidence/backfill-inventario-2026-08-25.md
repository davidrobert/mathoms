backfill: 64 SHA(s) de bypass, 53 sem gate
`f28051e4` (PR #1304) — **late**: `All checks green` só concluiu 319s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`1d877f74` (PR #1307) — **late**: `All checks green` só concluiu 689s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`6354427e` (PR #1320) — **late**: `All checks green` só concluiu 534s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`11b7f700` (PR #1318) — **late**: `All checks green` só concluiu 701s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`0d4fca9f` (PR #1315) — **late**: `All checks green` só concluiu 691s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`e0ad6131` (PR #1328) — **late**: `All checks green` só concluiu 22s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`247810bb` (PR #1322) — **late**: `All checks green` só concluiu 819s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`9876292e` (PR #1323) — **late**: `All checks green` só concluiu 22s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`3376f343` (PR #1331) — **late**: `All checks green` só concluiu 522s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`1b3888ee` (PR #1332) — **late**: `All checks green` só concluiu 743s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`d1b7c97c` (PR #1393) — **late**: `All checks green` só concluiu 646s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`45d727d3` (PR #1384) — **late**: `All checks green` só concluiu 644s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`70407cc3` (PR #1387) — **late**: `All checks green` só concluiu 637s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`703319b2` (PR #1394) — **late**: `All checks green` só concluiu 573s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`a920541f` (PR #1402) — **late**: `All checks green` só concluiu 720s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`9dd59380` (PR #1400) — **late**: `All checks green` só concluiu 503s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`f321cd51` (PR #1404) — **late**: `All checks green` só concluiu 615s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`7784ba33` (PR #1399) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`608163ef` (PR #1395) — **late**: `All checks green` só concluiu 712s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`baa88213` (PR #1450) — **late**: `All checks green` só concluiu 953s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`8934b00d` (PR #1453) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`69366a93` (PR #1458) — **late**: `All checks green` só concluiu 943s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`5d3bb01b` (PR #1459) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`8d07c4fb` (PR #1462) — **late**: `All checks green` só concluiu 357s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`09346f47` (PR #1465) — **late**: `All checks green` só concluiu 141s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`0f01c6b3` (PR #1500) — **late**: `All checks green` só concluiu 180s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`d1153a66` (PR #1493) — **late**: `All checks green` só concluiu 180s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`a9dc25af` (PR #1503) — **late**: `All checks green` só concluiu 720s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`253194a5` (PR #1504) — **late**: `All checks green` só concluiu 167s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`2e79a378` (PR #1506) — **late**: `All checks green` só concluiu 60s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`8ad3af9e` (PR #1494) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`708c2348` (PR #1505) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`0f35fbf3` (PR #1508) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
`72b89113` (PR #1517) — **late**: `All checks green` só concluiu 106s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`bfc9c522` (PR #1532) — **late**: `All checks green` só concluiu 149s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`de8cae88` (PR #1533) — **late**: `All checks green` só concluiu 827s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`c7c09021` (PR #1536) — **late**: `All checks green` só concluiu 114s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`7877feb5` (PR #1577) — **late**: `All checks green` só concluiu 147s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`1467efa1` (PR #1595) — **late**: `All checks green` só concluiu 50s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`0daecc79` (PR #1601) — **late**: `All checks green` só concluiu 148s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`b1b49e6b` (PR #1623) — **late**: `All checks green` só concluiu 65s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`9ee6de55` (PR #1615) — **late**: `All checks green` só concluiu 442s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`32d00cec` (PR #1612) — **late**: `All checks green` só concluiu 152s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`e6b3e85f` (PR #1622) — **late**: `All checks green` só concluiu 158s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`bed3cc01` (PR #1627) — **late**: `All checks green` só concluiu 128s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`55539d6e` (PR #1626) — **late**: `All checks green` só concluiu 104s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`daf9e67e` (PR #1628) — **late**: `All checks green` só concluiu 183s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`1b0036f5` (PR #1629) — **late**: `All checks green` só concluiu 20s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`88b9eb8d` (PR #1573) — **late**: `All checks green` só concluiu 306s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`64df6c60` (PR #1658) — **late**: `All checks green` só concluiu 128s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`d5d9a7fb` (PR #1666) — **late**: `All checks green` só concluiu 39s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`ffdac5d9` (PR #1699) — **late**: `All checks green` só concluiu 43s DEPOIS do merge · bypass do Ruleset por `davidrobert`
`a3aa4c63` (PR #1701) — **red**: `All checks green` = failure no head · bypass do Ruleset por `davidrobert`
