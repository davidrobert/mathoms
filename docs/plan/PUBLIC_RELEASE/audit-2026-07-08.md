# PUBLIC_RELEASE — Inventário de auditoria PII/segredos (2026-07-08)

> **Anexo do plano PUBLIC_RELEASE** (`docs/plan/PUBLIC_RELEASE/_README.md`, criado a seguir).
> Fonte: auditoria multi-agente de 2026-07-08 (histórico git completo · working
> tree · apresentação · sre-devops), re-confirmada por `git ls-files` no mesmo dia.
>
> **Regra de dados:** este documento MASCARA todo dado real (CPF `***.***.***-NN`,
> valores como "~R$ NNNk", endereços como "Rua T*** / Praça B***"). NÃO cole dado
> real aqui — este arquivo será público. Ele é o **mapa** do saneamento, não a evidência.
>
> **Como usar:** cada lane de saneamento (Onda 1) referencia a subseção
> correspondente. Ao executar, re-varra (`git grep`, `git log --all -S`) para pegar
> ocorrências que surgiram depois desta data — não confie só nesta lista.

---

## 0. Veredito

O repo tem contaminação de PII em **3 camadas**. O flip in-place seguro exige
tratar as três:

1. **HEAD atual** — dados reais em arquivos tracked (código, testes, docs, `_archive/`).
2. **Histórico git** — recuperável de `origin/main` mesmo após limpar o HEAD.
3. **Metadados GitHub** — 855 PRs/issues/logs de CI, imutáveis por git.

Veio **limpo**: zero API keys vivas, zero e-mail pessoal em conteúdo tracked,
fixtures golden `tests/fixtures/pipeline_golden/dogfood/` genuinamente sintéticas,
`config/prompts/*.yaml` sem dados reais.

---

## 1. Camada 1 — HEAD atual (arquivos tracked)

### 1.1 `_archive/` — deletar do HEAD inteiro (100 arquivos tracked)

| Alvo | Conteúdo | Severidade |
| --- | --- | --- |
| `_archive/pdf_backup_santander_itau_c6/` (58 PDFs) | Extratos bancários REAIS PF+PJ (Santander/Itaú/C6), faturas de cartão, extratos USD/EUR, posições de investimento, CDBs; nome+endereço embutidos no binário | CRÍTICO |
| `_archive/sessoes_anteriores/relatorio_financeiro_ferreira_campos_20260406.html` e `...20260408.html` | 2 relatórios financeiros completos reais da família | CRÍTICO |
| `_archive/legacy_scripts/STEP_6b_EXTRACTION_REPORT.txt` (e `STEP_6b_COMPLETION_SUMMARY.md`) | 2 CPFs reais do casal (dígito verificador válido) + nome + patrimônio | CRÍTICO |
| `_archive/manual_operacao_v6.1.md` | Dados reais espalhados: TED p/ familiar (nome), financiamento, empregador da cônjuge, nomes de solteira/casada + filho, inventário de docs (holerite, passaporte), imóveis, carteira | CRÍTICO |
| `_archive/tests_legacy/test_e5_patrimonio_formats.py` | Os mesmos 2 CPFs reais | CRÍTICO |
| `_archive/pre-f8-cutover-2026-04-15/config/tarefas.md` | 43 tarefas pessoais reais (FGTS, procurações, testamento, IPTU por imóvel, plano EUA) | CRÍTICO |
| `_archive/sessoes_anteriores/auditoria_scripts_pipeline.xlsx` | Binário não auditado — assumir contaminado | MÉDIO |
| `_archive/legacy_scripts/diagnostico_*.md`, `extract_members*.py` | Nomes hardcoded | ALTO |

**Ação:** `git rm -r _archive/` no HEAD (o histórico é tratado na Onda 3). Nada em
`_archive/` é recuperável por redação pontual.

### 1.2 `EXEMPLO_DE_RELATORIO.html` — regenerar sintético (dependência de renderer)

- `docs/plan/REPORT_PREMIUM/EXEMPLO_DE_RELATORIO.html` (10.106 linhas, ~400 valores R$):
  relatório REAL do casal — nomes, idades, empregadores, salário da cônjuge,
  **endereço residencial**, patrimônio, plano de imigração EUA (F1/F2, EB2-NIW, NCLEX),
  nomes dos pets.
- **Dependência:** é a referência de paridade React do plano REPORT_PREMIUM
  (`docs/plan/REPORT_PREMIUM/_README.md` linka-o). Regenerar exige persona 100%
  sintética que exercite as mesmas seções/componentes do renderer — não é delete simples.
- Severidade: **CRÍTICO**.

### 1.3 Endereço residencial real em ~27 arquivos (código + testes + docs)

Rua T*** da S***, 61 (+ variação Praça B*** C*** 190/186, apto 34) aparece em:

- **Código/testes de produção (neutralizar com fixture sintética):**
  `backend/tests/test_property_api.py`, `backend/tests/test_property_identity_model.py`,
  `backend/tests/test_db_property_identity_resolver.py`,
  `frontend/tests/components/RealEstateYieldCard.test.tsx`,
  `pipeline/llm/prompts/apolice.py` (exemplo no prompt LLM),
  `pipeline/domain/services/endereco_canonicalizer.py` (comentário).
- **Docs (anonimizar):** `docs/adr/215-*.md`, `docs/adr/225-*.md`, `docs/adr/239-*.md`,
  `docs/adr/246-*.md`, `docs/adr/265-*.md`, `docs/plan/RESIDENCIA_E_USO/_README.md`,
  `docs/agent_prompts/orchestrator_a17_a18_a19.md`,
  `docs/sprint/A18/lanes/A18-l2-apolice.md`, `docs/sprint/A18/tracks/a18-l2-apolice-seguro.md`.
- Matrícula real `4NN.NNN` + "COND. L*** W***" em `docs/adr/246-*.md`.
- Severidade: **CRÍTICO**.

### 1.4 Placas de veículo reais

- 3 placas reais (com marca/modelo/ano) em `docs/adr/239-*.md`,
  `docs/sprint/A18/lanes/A18-l1-crlv.md`, `docs/sprint/A18/lanes/A18-l2-apolice.md`,
  changelog A18, `docs/agent_prompts/orchestrator_a17_a18_a19.md`.
- Severidade: **ALTO**.

### 1.5 Código com nomes de terceiros e dados fiscais reais

| Alvo | Conteúdo | Severidade |
| --- | --- | --- |
| `backend/alembic/versions/a5b6c7d8e9f0_seed_category_template_v1.py` | Migration de seed de PRODUÇÃO com keywords de categorização = nomes reais de terceiros (sogra/sogro/diarista), empregadores nominais, mapping "Einstein (M*** - CLT)" / "Kiwify (D*** - CLT)" | CRÍTICO |
| `backend/app/scripts/seed_if_goal_ferreira_campos.py` | Sobrenome da família no filename + metas financeiras reais (IF ~R$ 7,2M, renda passiva ~R$ 30k/mês) | ALTO |
| `config/report_spec.md` | Linhas de orçamento com nomes de terceiros + situação fiscal real (DAS em atraso, carnê-leão) | ALTO |
| `config/agents/planner_persona.md` / `docs/adr/304-*.md` | "renda tributável de R$ 720.000" possivelmente derivada de dado real — trocar por número sintético | BAIXO |

### 1.6 ADRs e docs de sprint com valores/transações reais (anonimizar texto)

- `docs/adr/255-*.md`: transações reais de salário PJ (Pix de empregador nomeado,
  ~R$ 47k/mês), clientes PJ nomeados, **nome completo de diarista**.
- `docs/adr/267-*.md`: variantes do nome do dono, patrimônio da cônjuge por ano
  (~R$ 811k/988k), totais do workspace (~R$ 4,0M→3,3M).
- `docs/adr/244-*.md`: slug de membro = nome completo + CDB ~R$ 290k + dívidas.
- `docs/adr/261-*.md`: número de contrato de crédito imobiliário real + parcela.
- `docs/adr/245-*.md`: valores exatos de moeda estrangeira do IRPF.
- `docs/adr/241-*.md`, `docs/adr/266-*.md`: roster de bancos + renda do workspace.
- `docs/adr/073-*.md`, `docs/adr/136-*.md`, `docs/adr/168-*.md`: metas reais + plano EUA + quitação de financiamento.
- `docs/sprint/A17/lanes/A17-l6-*.md`: decomposição real de renda (lucros distribuídos, aluguéis, CLT).
- `docs/sprint/A28/lanes/A28-l5*.md`, `docs/sprint/A12/changelog/CHG-2026-05-12*.md`
  (revela filhos), `docs/sprint/A8/tracks/a8-trs-real.md`.
- `docs/plan/COMPETITIVE_PIERRE/assets/3e-discovery-2026-05-23.md`: valores plausivelmente reais.
- **Cuidado com wikilinks:** ADRs 246/255/267/271 são `adrs_canonical` de
  [[PLAN-launch-trust]] — anonimizar o CORPO sem quebrar `id`/wikilinks/âncoras.

### 1.7 CPF real em docs de archive

- `docs/archive/BACKLOG-pre-shim-2026-05-07.md` e
  `docs/sprint/_archive_pre_a6/_README.md`: transcrevem o CPF real do founder em
  texto corrido (ironicamente, no doc que celebra a remoção de CPFs). Severidade: **CRÍTICO**.

### 1.8 Paths de máquina local

- `dev/code_style_baseline.json`, `dev/dedup_property_identity.py`:
  `/Users/davidrobert/...` vaza username/estrutura local. Severidade: **BAIXO**.

---

## 2. Camada 2 — histórico git (recuperável de `origin/main`)

Mesmo com o HEAD limpo, tudo abaixo é alcançável via `git show <hash>:<path>`.
Hashes conhecidos (para o runbook de rewrite da Onda 3):

| Alvo histórico | Hash(es) | Conteúdo |
| --- | --- | --- |
| `.env` + `backend/.env` com Fernet key REAL | entrou `ae340c60` · removido `90279c68` ("chave rotacionada") | Confirmar que a key antiga não decripta nada em prod |
| `config/family_members.json` | `e55fb489` … deletado `07c35113` (A7.5) | CPFs, datas nasc, FILHO MENOR c/ dupla cidadania, endereço, profissões, empregador |
| `members/` (holerite + currículos) | `19defb46` → `28308ff8` | Holerite da cônjuge (salário, agência/conta, matrícula), currículos (telefone, e-mail, LinkedIn) |
| `processed/` (326 paths) | snapshots "pre-full-reset" | Artefatos E2→E5 com transações reais; `despesas-4_unified.json` ~1 MB × ~22 versões |
| `config/decisions.md` | até `ffc63f0b` (A7.2a) | Decisões financeiras reais (imóveis nomeados, dívidas, aportes, meta IF, plano EUA) |
| `config/goals.json` | até A10 | Metas reais (aportes, veículos, dolarização, tuition/moradia EUA) |
| `config/categorization.json` | histórico | Keywords de estabelecimentos reais (hábitos), mapa PJ/CLT |
| `PDFs bancários` (`_archive/...`) | entraram `e7b40d30` | 12,5 MB de PDFs reais (também no HEAD — ver §1.1) |

**Mensagens de commit (imutáveis sem `--replace-message`):** ~100 mensagens com
nome completo da família; ~20 com valores reais (patrimônio por member_key).
`git filter-repo` de blob NÃO limpa mensagem — precisa de `--replace-message`.

**Identidades de autor:** `davidrobert@gmail.com` em 813 commits (autor) + 26
(committer). `david@arvo.com.br` em 1.902 commits. Decisão de mailmap na Onda 0/3.

**Branches remotas:** 93 `origin/agent/*` — também ficam públicas; deletar antes/durante o flip.

---

## 3. Camada 3 — metadados GitHub (imutáveis por git)

- **855 PRs**, issues (incl. "Security schedule failure"), comentários e logs de
  CI (retention ~90d) com diagnósticos de dogfood (valores reais) e discussão de
  estratégia competitiva (COMPETITIVE_PIERRE).
- `git filter-repo`/BFG NÃO alcançam isto. Só resolve: (a) edição/deleção manual
  dos itens sensíveis antes do flip, ou (b) aceite de risco documentado do owner.
- É o risco de maior blast-radius e o único que o rewrite de git não cobre.

---

## 4. Gate anti-regressão (o buraco que deixou isso passar)

- `tests/utils/lint_no_real_pii.py` cobre `tests/` mas **NÃO** `docs/` nem `_archive/`.
- `dev/check_forbidden_paths.py` bloqueia `storage/`, `data/`, `inbox/`, `_scratch/`,
  `.env`, `*.db`, configs migradas — mas **NÃO** `_archive/`.
- **Sem estender esses dois gates, o saneamento regride.** (Onda 2.)

---

## 5. Superfície operacional (sre-devops) — resumo

- Workflows sem footguns críticos (zero `pull_request_target`/`workflow_run`/self-hosted).
- Fernet key em `ci.yml:87` é dummy de teste (`# gitleaks:allow`), não prod.
- 4 actions de terceiros por tag flutuante em contexto write (SHA-pin na Onda 5):
  `chinthakagodawita/autoupdate-action` (docker, `contents: write`),
  `amannn/action-semantic-pull-request`, `actions/labeler`, `CodelyTV/pr-size-labeler`.
- **Tensão:** rewrite de histórico (Onda 3) exige force-push em `main`, que colide
  com o Ruleset `main-protection` (`non_fast_forward`) → bypass temporário pelo owner.
- Repo público → Actions ilimitado (resolve budget-block histórico) + GHAS grátis.

---

## 6. Verificação (critério de "limpo")

Antes do flip, `git grep` no HEAD deve retornar **zero** para: CPF com dígito
válido, o endereço residencial, placas, nomes de terceiros/família em contexto
financeiro; gitleaks limpo no histórico pós-rewrite; gates anti-PII cobrindo `docs/`.
Ver critério de aceite global no plano PUBLIC_RELEASE (`_README.md`, Onda 8).
