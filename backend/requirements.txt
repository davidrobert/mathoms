# Fin Backend — Python dependencies
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
alembic>=1.13
PyJWT>=2.10
cryptography>=43.0
bcrypt>=4.0
python-multipart>=0.0.7
pydantic[email]>=2.0
pydantic-settings>=2.0
aiosqlite>=0.20
# Drivers Postgres:
# - asyncpg: usado pelo async engine (backend/app/core/database.py:52) quando
#   MATHOMS_DATABASE_URL=postgresql+asyncpg://... — sem ele o import do
#   create_async_engine quebra antes mesmo do alembic abrir conexão.
# - psycopg2-binary: usado pelo sync_engine (database.py:21), que reaproveita a
#   mesma URL com `+asyncpg` removido (config.py:91) → resolve para psycopg2.
asyncpg>=0.29
psycopg2-binary>=2.9
greenlet>=3.0
httpx>=0.27
pyyaml>=6.0

# Phase 4 — LLM
litellm>=1.40.0
instructor>=1.3.0
pdfplumber>=0.11.0
eval_type_backport>=0.2.0  # Required for instructor on Python 3.9

# Spreadsheet parsing (used by _extract_file_preview in scripts/e0_route.py)
openpyxl>=3.1.0   # .xlsx files
xlrd>=2.0.1       # .xls files (OLE2/BIFF format — xlrd 2.x dropped xlsx support intentionally)

# Phase 9 — Server-side PDF via Playwright (ADR-076)
playwright>=1.47.0

# Phase 5 — Task Queue + Real-time
celery[redis]>=5.4.0
redis>=5.0.0
websockets>=12.0

# A6f.3 — Structured JSON logs + OpenTelemetry (ADR-110)
python-json-logger>=3.2
opentelemetry-api>=1.30
opentelemetry-sdk>=1.30
opentelemetry-exporter-otlp-proto-http>=1.30
opentelemetry-instrumentation-fastapi>=0.50b0
opentelemetry-instrumentation-sqlalchemy>=0.50b0
opentelemetry-instrumentation-celery>=0.50b0
opentelemetry-instrumentation-logging>=0.50b0

# Monte Carlo simulations (pipeline/if_projector.py imported transitively)
numpy>=1.26

# Dev/test
pytest>=8.0
pytest-asyncio>=0.24
