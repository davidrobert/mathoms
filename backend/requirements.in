# Fin Backend — Python dependencies (human-edited source · ADR-254)
# NÃO instalar diretamente: o build de prod usa o lock combinado da raiz
# `requirements.lock` (gerado por `pip-compile --generate-hashes` sobre
# requirements.in + backend/requirements.in num container linux/amd64).
# Regenerar: ver docs/reference/runbooks/python_dependencies.md
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
alembic>=1.13
PyJWT>=2.10
cryptography>=48.0.1  # SEC-03: GHSA-537c-gmf6-5ccf (OpenSSL estático no wheel <48.0.1)
bcrypt>=4.0
python-multipart>=0.0.31  # SEC-03: CVE-2026-53538/53539/53540 (DoS parse de form/upload)
pydantic[email]>=2.0
pydantic-settings>=2.0
aiosqlite>=0.20
# Drivers Postgres (ADR-253):
# - asyncpg: async engine (backend/app/core/database.py) p/ endpoints FastAPI
#   + Alembic (env.py já roda async) quando DATABASE_URL=postgresql+asyncpg://.
# - psycopg[binary] v3: sync_engine, que drivea o task layer Celery (prefork,
#   síncrono) + scripts. config.py.sync_database_url emite postgresql+psycopg://
#   explícito. Wheel binário traz libpq embarcado → runtime sem libpq5 apt dep.
#   psycopg2-binary (legado, modo manutenção) saiu em A20.L8.
asyncpg>=0.29
psycopg[binary]>=3.2
greenlet>=3.0
httpx>=0.27
pyyaml>=6.0

# Phase 4 — LLM
litellm>=1.40.0
instructor>=1.3.0
pdfplumber>=0.11.0
eval_type_backport>=0.2.0  # Required for instructor on Python 3.9

# Spreadsheet parsing (used by _extract_file_preview in scripts/route_documents.py)
openpyxl>=3.1.0   # .xlsx files
xlrd>=2.0.1       # .xls files (OLE2/BIFF format — xlrd 2.x dropped xlsx support intentionally)

# Phase 9 — Server-side PDF via Playwright (ADR-076)
playwright>=1.47.0

# Phase 5 — Task Queue + Real-time
celery[redis]>=5.4.0
redis>=5.0.0
websockets>=12.0

# A6f.3 — Structured JSON logs + OpenTelemetry (ADR-110)
python-json-logger>=3.2
opentelemetry-api>=1.30
opentelemetry-sdk>=1.30
opentelemetry-exporter-otlp-proto-http>=1.30
opentelemetry-instrumentation-fastapi>=0.50b0
opentelemetry-instrumentation-sqlalchemy>=0.50b0
opentelemetry-instrumentation-celery>=0.50b0
opentelemetry-instrumentation-logging>=0.50b0

# Monte Carlo simulations (pipeline/if_projector.py imported transitively)
# Teto de major por ADR-360 — ver comentário em requirements.in da raiz.
numpy>=1.26,<3

# Dev/test
pytest>=8.0
pytest-asyncio>=0.24
