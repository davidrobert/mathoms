"""Use cases do agregado ``Category``.

Pós-sunset do CRUD legado ``/config/categories`` (A12.cat-legacy-sunset),
o agregado é servido pelo caminho de overrides — template global +
``workspace_category_overrides`` (ADR-137 · ADR-185). Use cases em
:mod:`backend.app.application.category.override_use_cases`.
"""
